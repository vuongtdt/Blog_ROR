<!DOCTYPE html>
<html>
<head>
    <title>My Site</title>
</head>
<body>
    <div class="container">
    <% if @post.user.id == current_user.id%>
        <div class="controller">
            <%= button_to "Edit Post", edit_post_path(@post), method: :get%>
            <%= button_to "Delete Post", @post, method: :delete %>
        </div>
    <% end %>
        <h1><%= @post.title%></h1>
        <h3 class="author">Author: <%= @post.user.email%></h3>
        <h3 class="status"><%= @post.status%></h3>

        <p class="content"><%= @post.body%></p>

        <%= link_to "Back to Home", root_path, class:"back_link"%>
    </div>
</body>
</html>
<style>
    table, th, td {
        border: 1px solid black;
        border-collapse: collapse;
    }
    th, td {
        padding: 15px;
        min-width: 100px;
        text-align: center;
    }

    th {
        color: green;
    }
    .container {
        position: relative;
        display: flex;
        flex-wrap: wrap;
        min-width: 960px;
        max-width: 960px;
        margin-left: auto;
        margin-right: auto;
    }
    h1, h3, p{
        width: 100%;
    }
    .controller{
        position: absolute;
        display: flex;
        width: 180px;
        top: 10px;
        right: 10px;
        justify-content: space-between;
    }
    .author{
        font-size: 14px;
        color: #696969;
        font-style: italic;
        margin-top: 10px;
    }
    .status {
        color: #500;
        font-size: 10px;
    }
    .content{
        padding-top: 20px;
        font-size: 16px;
        text-wrap: break-word;
    }
    .back_link{
        margin-top: 30px;
    }
</style>